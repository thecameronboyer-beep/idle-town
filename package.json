{
  "name": "idle-town",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "build:assets": "vite build --config vite.assets.config.ts",
    "update:assets": "node ./scripts/update-assets-app.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "preview:lan": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^8.0.11"
  }
}
